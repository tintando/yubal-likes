# Media

The fork's own images, used by the top-level `README.md`. Upstream's `docs/demo.gif` and `extension/docs/images/` are upstream's and are left where upstream keeps them.

| File | What it shows |
|---|---|
| `home.png` | The hero: the Sync page as it opens, with the three actions, the last-synced, track-count and next-sync cards and the recent syncs list, under a header carrying the fork's Likes and Library tabs and its Drive and cookie icons |
| `orphan-review.png` | The orphan review panel: unreferenced files, two of them paired with the newer upload that replaced them, and the per-file delete, keep and never-delete actions |
| `drive-menu.png` | The Drive icon's menu: upload library now, re-authorize, upload new client secrets, disconnect |
| `cookie-menu.png` | The cookie icon's menu: switch account, upload new cookies, delete cookies |

Dark theme, no browser chrome. The two menu shots are cropped to the menu and the icon that opened it.
